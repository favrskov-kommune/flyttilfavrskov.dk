{#
/**
 * @file
 * Default theme implementation for a three-column layout.
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 */
#}

{% set classes = [
  'layout',
  'layout--threecol',
  settings.color_theme != 'none' ? 'theme theme-' ~ settings.color_theme,
  'section',
  'section--spacing-top-' ~ settings.column_spacing_top,
  'section--spacing-bottom-' ~ settings.column_spacing_bottom
] %}

<section {{ attributes.addClass(classes) }}>
  <div class="container">
    <div{{ create_attribute({'class': 'row'}).addClass(settings.vertical_align ? 'middle-xs') }}>
      <div {{ region_attributes.first.addClass(['col-xs-12', 'col-md-4', 'layout__region']) }}>
        {{ content.first }}
      </div>
      <div {{ region_attributes.second.addClass(['col-xs-12', 'col-md-4', 'layout__region']) }}>
        {{ content.second }}
      </div>
      <div {{ region_attributes.third.addClass(['col-xs-12', 'col-md-4', 'layout__region']) }}>
        {{ content.third }}
      </div>
    </div>
  </div>
</section>
