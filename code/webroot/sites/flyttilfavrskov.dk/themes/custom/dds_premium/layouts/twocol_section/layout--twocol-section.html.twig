{#
/**
 * @file
 * Default theme implementation to display a two-column layout.
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 */
#}

{% set columns = {
  '50-50': { left: 'col-xs-12 col-md-6', right: 'col-xs-12 col-md-6' },
  '33-67': { left: 'col-xs-12 col-md-4', right: 'col-xs-12 col-md-8' },
  '67-33': { left: 'col-xs-12 col-md-8', right: 'col-xs-12 col-md-4' },
  '25-75': { left: 'col-xs-12 col-md-3', right: 'col-xs-12 col-md-9' },
  '75-25': { left: 'col-xs-12 col-md-9', right: 'col-xs-12 col-md-3' }
} %}

{% set column_style = settings.column_widths ? settings.column_widths : '50-50' %}
{% set allow_spacing = settings.column_spacing and settings.column_spacing != 'none' %}

{% set classes = [
  'section',
  settings.color_theme != 'none' ? 'theme theme-' ~ settings.color_theme,
  not allow_spacing ? 'section--no-spacing'
] %}


{% if content %}
  <section {{ attributes.addClass(classes) }}>
    <div class="container">
      <div class="row">
        {% if content.first %}
          <div {{ region_attributes.first.addClass('layout__region', 'layout__region--first', columns[column_style].left ) }}>
            {{ content.first }}
          </div>
        {% endif %}

        {% if content.second %}
          <div {{ region_attributes.second.addClass('layout__region', 'layout__region--second', columns[column_style].right) }}>
            {{ content.second }}
          </div>
        {% endif %}
      </div>
    </div>
  </section>
{% endif %}
